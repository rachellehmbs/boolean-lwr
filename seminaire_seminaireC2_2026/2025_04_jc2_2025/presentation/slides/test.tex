% !TEX root = ../slides.tex

\begin{frame}[label=]
\frametitle{The enigmatic Kim function}

\begin{mybox}[corange]{Subspace property}
	Let $\FF \subset \LL$ and $F \from \LL \to \LL$. $F$ verifies the subspace property if:
	$$ \forall \ \lambda, \quad F(\lambda \FF) \subset F(\lambda) \FF $$
\end{mybox}

\begin{mybox}[cblue]{Subspace property}
	Let $\FF \subset \LL$ and $F \from \LL \to \LL$. $F$ verifies the subspace property if:
	$$ \forall \ \lambda, \quad F(\lambda \FF) \subset F(\lambda) \FF $$
\end{mybox}

\begin{mybox}[cgreen]{Subspace property}
	Let $\FF \subset \LL$ and $F \from \LL \to \LL$. $F$ verifies the subspace property if:
	$$ \forall \ \lambda, \quad F(\lambda \FF) \subset F(\lambda) \FF $$
\end{mybox}

\begin{mybox}[cred]{Subspace property}
	Let $\FF \subset \LL$ and $F \from \LL \to \LL$. $F$ verifies the subspace property if:
	$$ \forall \ \lambda, \quad F(\lambda \FF) \subset F(\lambda) \FF $$
\end{mybox}
\end{frame}